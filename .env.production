# Variables de entorno para PRODUCCIÓN
# Este archivo es un TEMPLATE - NO subir al repositorio con valores reales
# Copia este archivo como .env en tu VPS y cambia los valores

PORT=3002
AUTH_TOKEN=CAMBIAR-POR-TOKEN-REAL-SUPER-SEGURO
NODE_ENV=production

# URLs y configuraciones de producción
BASE_URL=https://tu-dominio.com

# Configuración de producción
DEBUG=false

# CONFIGURACIÓN SSL:
# En producción (NODE_ENV=production) se intentará cargar certificados SSL automáticamente
# Ruta de certificados: /etc/letsencrypt/live/tu-dominio/
# Si no existen, fallback a HTTP

# INSTRUCCIONES PARA EL VPS:
# 1. Copia este archivo como .env
# 2. Cambia AUTH_TOKEN por un valor real y seguro
# 3. Ajusta PORT según tu configuración de nginx/apache
# 4. Cambia BASE_URL por tu dominio real
# 5. Asegúrate de tener certificados SSL en /etc/letsencrypt/ o configura nginx como proxy
